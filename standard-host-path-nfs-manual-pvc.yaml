apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: standard-host-path-nfs-manual-pvc
  namespace: test
spec:
  storageClassName: standard
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 128Mi
